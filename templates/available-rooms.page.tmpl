{{template "base" .}}

{{define "content"}}
<div class="mx-auto mt-5 p-5">
    <h1> Available Rooms </h1>

    {{$rooms := index .Data "rooms"}}
    {{range $rooms}}

    {{.RoomName}} <br>

    {{end}}

</div>

{{end}}